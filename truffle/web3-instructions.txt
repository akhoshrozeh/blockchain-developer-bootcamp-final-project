// instantiate the factory
let factory = await ProducerFactory.deployed()

// get producers
factory.getProducers()


// create a producer; it will return the producer object
factory.createProducer("Dj flow")


// get the address of the producer contract created!
let prods = await factory.getProducers()
let p0 = prods[0]

// save the abi for producer from artifacts
let fs = require('fs');
let jsonFile = "./build/contracts/Producer.json";
let parsed= JSON.parse(fs.readFileSync(jsonFile));
let prodABI = parsed.abi;

// create the contract object from abi and address
let p0Contract = new web3.eth.Contract(prodABI, p0)

// calling a view function with no params
p0Contract.methods.aFunction().call()

// other way 
p0__Contract.methods.aFunction(param1, ...).send({from: accounts[0], gas: 1000000})